services:
  - type: web
    name: karada-map-backend
    env: node
    plan: free
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: ORS_API_KEY
        sync: false
      - key: CORS_ORIGIN
        sync: false
